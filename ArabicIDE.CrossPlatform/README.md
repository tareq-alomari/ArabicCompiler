# 🚀 بيئة التطوير العربية متعددة المنصات (Avalonia)

هذه المجلد يحتوي على مشروع **ArabicIDE.CrossPlatform** المبني بإطار العمل Avalonia لتوفير تجربة استخدام عربية كاملة على أنظمة **macOS** و **Windows** و **Linux** بدون التأثير على تطبيق ‎WinForms القديم.

---

## 🎯 نظرة عامة

- واجهة عربية بالكامل مع دعم اتجاه الكتابة من اليمين لليسار للعناصر الرسومية.
- محرر أكواد متقدم يعتمد على **AvaloniaEdit** مع تمييز نحوي مخصص للغة البرمجة العربية.
- أوامر متكاملة للترجمة والتشغيل تستدعي المترجم الأصلي المكتوب بـ ‎C++‎ وتنفّذ كود ‎C‎ الناتج عبر `gcc` أو `clang`.
- بنية `MVVM` واضحة تفصل الواجهة عن منطق الأعمال والخدمات.

---

## ✨ المزايا الرئيسية

- **فتح / حفظ / حفظ باسم** مع تتبع لحالة التعديلات غير المحفوظة.
- **شريط قوائم وأشرطة أدوات عربية** مع اختصارات قياسية (`Ctrl+N`, `Ctrl+S`, `F5`).  
- **محرر AvaloniaEdit** يدعم التراجع، تحديد النص، أرقام الأسطر، وملف التمييز `ArabicSyntax.xshd`.
- **وحدة المخرجات** تعرض رسائل المترجم وتشغيل البرنامج بالعربية مع زر لمسح السجل.
- **خدمات مستقلة** للتعامل مع الملفات (`IFileService`) واستدعاء المترجم (`ICompilerService`).  
- كل الملفات تتبع UTF-8 لضمان ظهور النص العربي بشكل صحيح.

---

## 🔧 المتطلبات

| المكون | الإصدار المقترح |
|--------|-----------------|
| ‎.NET SDK‎ | ‎9.0‎ أو أحدث |
| Avalonia | يتم جلبها آليًا عبر ‎NuGet‎ |
| المترجم العربي | مبني مسبقًا في ‎`Compiler/build/ArabicCompiler`‎ |
| gcc / clang | لتجميع كود ‎C‎ الناتج |
| macOS | يلزم `xcode-select --install` و `brew install cmake` |

> يمكن تشغيل المشروع على Windows أو Linux بشرط توفر ‎.NET SDK‎ ومترجم ‎C++‎ متوافق.

---

## ▶️ خطوات البناء والتشغيل

```bash
# 1) الانتقال إلى المجلد
cd ArabicIDE.CrossPlatform

# 2) استعادة الحزم وبناء التطبيق
dotnet restore
dotnet build

# 3) تشغيل بيئة التطوير
dotnet run
```

بعد تشغيل التطبيق:
1. اكتب الكود العربي في المحرر أو افتح ملفًا موجودًا.  
2. اضغط **`ترجم (Compile)`** لتوليد ملف ‎C‎ مؤقت.  
3. اضغط **`شغّل (Run)`** لتجميع الملف عبر `gcc` وتشغيل الناتج وعرض المخرجات بالعربية.

> يجب أن يكون المترجم العربي المبني متاحًا في المسار الافتراضي: `../../Compiler/build/ArabicCompiler` بالنسبة لمجلد المشروع.

---

## 🗂️ بنية المجلد

```
ArabicIDE.CrossPlatform/
├── App.axaml                # تعريف الأنماط والموارد العامة
├── App.axaml.cs             # نقطة بدء تطبيق Avalonia
├── Program.cs               # تهيئة AppBuilder وتشغيل النافذة
├── ArabicSyntax.xshd        # ملف التمييز النحوي للغة العربية
├── Services/                # خدمات المترجم والملفات
│   ├── CompilerService.cs
│   ├── FileService.cs
│   ├── ICompilerService.cs
│   └── IFileService.cs
├── ViewModels/              # منطق العرض (MVVM)
│   ├── MainWindowViewModel.cs
│   └── ViewModelBase.cs
├── Views/                   # ملفات XAML للواجهة
│   ├── MainWindow.axaml
│   └── MainWindow.axaml.cs
└── Assets/                  # الأيقونات والموارد الرسومية
```

---

## 🔁 تكامل المترجم

- `CompilerService` يقوم بإنشاء ملف مؤقت بامتداد ‎`.arabic`‎ ويمرره إلى المترجم C++ مع خيار ‎`--c`‎.
- عند النجاح يتم حفظ مسار ملف ‎C‎ الناتج لتمكين أمر **Run**.  
- زر **Run** يستدعي `gcc` لبناء الملف التنفيذي المؤقت ثم يعرض مخرجاته داخل نافذة التطبيق.
- جميع العمليات تعمل داخل عمليات منفصلة (`System.Diagnostics.Process`) مع ترميز UTF-8.

---

## ✅ التحقق والاختبارات

قبل رفع أي تغييرات:
1. نفّذ ‎`dotnet build`‎ للتأكد من نجاح البناء على المنصة الحالية.  
2. شغّل التطبيق وتحقق من:
   - فتح الملفات وحفظها.  
   - عمل الترجمة والتشغيل باستخدام مثال بسيط (مثل برنامج "مرحبا").  
   - ظهور التمييز النحوي والألوان بشكل صحيح.
3. تأكد من وجود ملف المترجم العربي في المسار المتوقّع وأن `gcc`/`clang` متوفر في متغير PATH.

> هذه الخطوات تمنع حدوث أي أعطال عند دمج الفرع في المستودع الرئيسي.

---

## 🛠️ استكشاف الأخطاء

| الرسالة | السبب المحتمل | الحل |
|--------|----------------|------|
| `❌ المترجم غير موجود` | لم يتم بناء ‎C++‎ أو المسار غير صحيح | شغّل `Compiler/build.sh` ثم حدّث المسار في `CompilerService` إذا لزم |
| `gcc: command not found` | ‎gcc‎ غير مثبت | قم بتثبيت `gcc` أو استخدم `clang` |
| عدم ظهور الألوان | ملف ‎`ArabicSyntax.xshd`‎ غير محمّل | تأكد من بقاء الملف مضمّنًا كـ EmbeddedResource |
| انقلب اتجاه النص | تغيير FlowDirection على TextArea | أزل أي تخصيص يدوي وأعد البناء |

---

## 🤝 المساهمة

- أضف الميزات الجديدة عبر ViewModel وخدمات مستقلة للحفاظ على النظافة المعمارية.  
- احرص على ترك تطبيق ‎WinForms‎ القديم دون تعديل لضمان **صفر تأثير** على المستخدمين الحاليين.  
- عند إضافة حزم جديدة استخدم ‎`dotnet restore`‎ وتأكد مِن أن النسخ تعمل على الأنظمة الثلاثة.

---

## 📬 الدعم

لتحميل تقارير الأعطال أو طلب تحسينات، يرجى استخدام قناة التواصل المعتمدة في المستودع الرئيسي (`Issues`). أرفق دائمًا مخرجات نافذة التطبيق والأوامر التي استُخدمت لتسهيل التشخيص.

بالتوفيق في بناء أفضل بيئة تطوير عربية! 🇸🇦✨


# دليل إصلاح المشاكل
## Problem Fixing Guide

**التاريخ**: 3 نوفمبر 2025

---

## 🔴 المشكلة 1: Crash بعد الطباعة

### الأعراض
```
Exit Code: -1073741819 (0xC0000005 - Access Violation)
```

### الخطوات التشخيصية

#### 1. تشغيل مع Visual Studio Debugger
```bash
# افتح المشروع في Visual Studio
# اضغط F5 لتشغيل مع debugger
# سيتوقف البرنامج عند الخطأ
```

#### 2. البحث عن المؤشرات الغير صحيحة
```cpp
// ابحث عن:
- delete على مؤشر null
- استخدام مؤشر بعد حذفه
- تجاوز حدود المصفوفات
```

#### 3. استخدام Memory Leak Detector
```bash
# استخدم Dr. Memory أو valgrind
drmemory.exe -- .\Compiler\build\Release\ArabicCompiler.exe .\Examples\calculator.arabic
```

### الحل المقترح

#### الخطوة 1: استخدام Smart Pointers
```cpp
// قبل:
ASTNode* root = parser.parse();
// ... استخدام root
delete root;  // قد ننسى هذا!

// بعد:
std::unique_ptr<ASTNode> root = parser.parse();
// سيتم تحرير الذاكرة تلقائياً
```

#### الخطوة 2: مراجعة main.cpp
```cpp
// ابحث عن:
int main() {
    // تأكد من تحرير جميع الموارد
    // استخدم try-catch
    // استخدم RAII pattern
}
```

#### الخطوة 3: اختبار الإصلاح
```bash
cd Compiler
cmake --build build --config Release
.\build\Release\ArabicCompiler.exe ..\Examples\calculator.arabic --ast
# يجب أن ينتهي بدون crash
```

---

## 🟡 المشكلة 2: ملفات فارغة

### الملف: hello.arabic

#### الحل
```bash
# افتح الملف
# أضف المحتوى التالي:
```

```arabic
برنامج مرحبا؛

اطبع "مرحبا بك في مترجم اللغة العربية!"؛

نهاية؛
```

#### الاختبار
```bash
.\Compiler\build\Release\ArabicCompiler.exe .\Examples\hello.arabic --ast
```

---

## 🟢 المشكلة 3: ملفات تحتاج اختبار

### الخطوات

#### 1. تشغيل الملف
```bash
.\Compiler\build\Release\ArabicCompiler.exe .\Examples\conditions.arabic --ast
```

#### 2. فحص النتيجة
- هل يطبع الشجرة النحوية بشكل صحيح؟
- هل يحدث crash؟
- هل هناك رسائل خطأ؟

#### 3. إصلاح الملف إذا لزم الأمر
- تحقق من الصيغة النحوية
- أضف الكلمات المحجوزة الناقصة
- أصلح الأخطاء

---

## 📋 قائمة التحقق

### قبل الإصلاح
- [ ] تشغيل البرنامج مع debugger
- [ ] تحديد موقع الخطأ
- [ ] فهم السبب

### أثناء الإصلاح
- [ ] تطبيق الحل
- [ ] اختبار الحل
- [ ] التحقق من عدم وجود آثار جانبية

### بعد الإصلاح
- [ ] تشغيل جميع الاختبارات
- [ ] التحقق من عدم وجود أخطاء جديدة
- [ ] توثيق التغييرات

---

## 🧪 اختبارات التحقق

### اختبار 1: عدم وجود Crash
```bash
.\Compiler\build\Release\ArabicCompiler.exe .\Examples\calculator.arabic --ast
echo "Exit Code: $LASTEXITCODE"
# يجب أن يكون 0
```

### اختبار 2: جميع الملفات
```bash
foreach ($file in Get-ChildItem "Examples\*.arabic") {
    .\Compiler\build\Release\ArabicCompiler.exe $file.FullName --ast
    if ($LASTEXITCODE -ne 0) {
        Write-Host "FAIL: $($file.Name)"
    }
}
```

### اختبار 3: الأداء
```bash
# قياس الوقت
Measure-Command {
    .\Compiler\build\Release\ArabicCompiler.exe .\Examples\calculator.arabic --ast
}
```

---

## 📚 الموارد المفيدة

### للتصحيح
- Visual Studio Debugger
- Dr. Memory
- valgrind

### للاختبار
- Google Test Framework
- CTest

### للتوثيق
- Doxygen
- Sphinx

---

## ✅ معايير النجاح

- [ ] لا يوجد crash
- [ ] جميع الملفات تعمل
- [ ] الأداء مقبول
- [ ] رسائل الخطأ واضحة

---

**تم إعداد الدليل بواسطة**: Augment Agent  
**التاريخ**: 3 نوفمبر 2025


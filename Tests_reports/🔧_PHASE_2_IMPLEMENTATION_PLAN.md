# 🔧 خطة تطبيق المرحلة 2
## Phase 2 Implementation Plan

**التاريخ**: 3 نوفمبر 2025  
**الحالة**: 🚀 جاري التطبيق  
**الهدف**: تحسين التوافق من 65% إلى 85%

---

## 📋 المهام الرئيسية

### 1. تحسين دعم الإجراءات ✅ (جاري)

#### 1.1 إضافة معاملات الإجراءات
- ✅ إضافة `ProcedureParameterNode` في AST.h
- ✅ إضافة `ProcedureDeclarationNode` في AST.h
- ✅ إضافة `ProcedureCallNode` في AST.h
- ⏳ تطبيق `parseProcedureDeclaration()` في Parser.cpp
- ⏳ تطبيق `parseProcedureParameters()` في Parser.cpp
- ⏳ تطبيق استدعاء الإجراءات في Parser.cpp

#### 1.2 دعم معاملات بالقيمة/بالمرجع
```arabic
إجراء اسم(بالقيمة س : صحيح) ؛
إجراء اسم(بالمرجع ص : حقيقي) ؛
```
- ⏳ تحديث Lexer لدعم كلمات محجوزة جديدة
- ⏳ تطبيق معالجة المعاملات في Parser

#### 1.3 دعم القيم المرجعة
```arabic
إجراء اسم() : صحيح ؛
  إرجاع 42 ؛
نهاية ؛
```
- ✅ إضافة `ReturnNode` في AST.h
- ⏳ تطبيق `parseReturnStatement()` في Parser.cpp

---

### 2. تحسين دعم السجلات ⏳

#### 2.1 الوصول لحقول السجلات
```arabic
متغير شخص : سجل { الاسم : خيط ؛ العمر : صحيح ؛ } ؛
شخص.الاسم = "أحمد" ؛
اطبع شخص.العمر ؛
```
- ✅ إضافة `FieldAccessNode` في AST.h
- ⏳ تطبيق `parsePostfix()` في Parser.cpp
- ⏳ تحديث `parsePrimary()` لدعم الوصول للحقول

#### 2.2 تحديث Compiler لمعالجة الحقول
- ⏳ تحديث Compiler.cpp لمعالجة FieldAccessNode
- ⏳ إضافة دعم الحقول في توليد الكود

---

### 3. اختبار شامل ⏳

#### 3.1 إنشاء ملفات اختبار جديدة
```
Examples/
├── procedure_basic.arabic
├── procedure_params.arabic
├── procedure_return.arabic
├── record_access.arabic
└── advanced_features.arabic
```

#### 3.2 تشغيل الاختبارات
- ⏳ اختبار الإجراءات الأساسية
- ⏳ اختبار المعاملات
- ⏳ اختبار القيم المرجعة
- ⏳ اختبار الوصول للحقول

---

## 📊 النتائج المتوقعة

| الميزة | الحالة الحالية | الحالة المتوقعة |
|--------|-----------------|-----------------|
| **الإجراءات** | 50% | 90% |
| **السجلات** | 50% | 85% |
| **المعاملات** | 0% | 80% |
| **القيم المرجعة** | 0% | 90% |
| **التوافق الإجمالي** | 65% | 85% |

---

## 🔄 الخطوات التفصيلية

### الخطوة 1: تحديث Lexer (إن لزم الأمر)
```cpp
// إضافة كلمات محجوزة جديدة
"بالقيمة" -> BY_VALUE
"بالمرجع" -> BY_REFERENCE
"إرجاع" -> RETURN
```

### الخطوة 2: تطبيق Parser
```cpp
// في Parser.cpp
std::unique_ptr<ProcedureDeclarationNode> Parser::parseProcedureDeclaration()
{
    // تطبيق معالجة الإجراءات
}

std::vector<ProcedureParameterNode> Parser::parseProcedureParameters()
{
    // تطبيق معالجة المعاملات
}

std::unique_ptr<ASTNode> Parser::parsePostfix()
{
    // تطبيق الوصول للحقول
}
```

### الخطوة 3: تحديث Compiler
```cpp
// في Compiler.cpp
void Compiler::visitProcedureDeclaration(ProcedureDeclarationNode* node)
{
    // توليد كود للإجراءات
}

void Compiler::visitFieldAccess(FieldAccessNode* node)
{
    // توليد كود للوصول للحقول
}
```

---

## ⏱️ الجدول الزمني

| المهمة | المدة | الحالة |
|--------|------|--------|
| تحديث AST | 30 دقيقة | ✅ مكتملة |
| تحديث Parser | 2 ساعة | ⏳ جاري |
| تحديث Compiler | 1.5 ساعة | ⏳ قادمة |
| الاختبار | 1 ساعة | ⏳ قادمة |
| **المجموع** | **4.5 ساعات** | ⏳ |

---

## 📝 الملاحظات

1. **الأولوية**: الإجراءات أولاً، ثم السجلات
2. **الاختبار**: اختبار كل ميزة بعد تطبيقها مباشرة
3. **التوثيق**: توثيق كل ميزة جديدة
4. **الاستقرار**: التأكد من عدم كسر الميزات الموجودة

---

## 🎯 الهدف النهائي

**تحسين التوافق من 65% إلى 85%** ✅

هذا سيجعل المترجم يدعم معظم الميزات المهمة ويكون جاهزاً للإصدار 1.0.

